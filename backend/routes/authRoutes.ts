import { Router, Request, Response } from 'express';

const router = Router();

// Placeholder for auth routes
router.get('/', (req: Request, res: Response) => res.send('Auth routes'));

export default router;
